<h1>Searching for - <%= @search_term %></h1>
<p>Powered By Food2Fork.com</p>
<% if (@recipes.nil? or @recipes == []) %>
<p> <h2>No recipes found... Sorry</h2></p>
<% else %>

<table border="1">
	<tr>
	<th>Image</th>
	<th>Title</th>
	<th>Rank</th>
	</tr>
	<% @recipes.each do |recipe| %>
	<tr class=<%= cycle('even', 'odd') %>>
	<td><%= link_to(image_tag(recipe["image_url"], height: 100, width: 100), recipe["FOOD2FORK_SERVER_AND_PORT"])%></td>
	<td><%= link_to(recipe["title"], recipe["FOOD2FORK_SERVER_AND_PORT"]) %> </td>
	<td><%= recipe["social_rank"] %></td>
	</tr>
	<% end %>
</table>
<% end %>
